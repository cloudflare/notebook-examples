{
    "name": "notebook-examples",
    "version": "0.1.1",
    "type": "module",
    "scripts": {
      "lint": "eslint --ignore-pattern 'eslint.config.*js' pages",
      "preview": "wrangler pages dev --port 8088 export",
      "deploy": "wrangler pages deploy --project-name notebook-examples export"
    },
    "dependencies": {
      "eslint": "^9.22.0",
      "wrangler": "^4.0.0",
      "dotenv": "^16.4.7"
    }
  }
